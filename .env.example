# Firebase config (including databaseURL) is hardcoded in
# packages/constants/src/FIREBASE_CONFIG.ts — no env vars needed.

# Server-only (apps/server)
PORT=4000
CLIENT_URL=http://127.0.0.1:8080
STRIPE_PRIVATE_KEY=
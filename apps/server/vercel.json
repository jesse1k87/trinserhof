{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "npm run build",
  "outputDirectory": "api",
  "builds": [{ "src": "./apps/server/dist/index.js", "use": "@vercel/node" }],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/apps/server/dist/"
    }
  ]
}

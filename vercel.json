{
  "buildCommand": "npm -w server run build",
  "installCommand": "npm install",
  "outputDirectory": "./apps/server/dist",
  "rewrites": [
    {
      "destination": "/api",
      "source": "/(.*)"
    }
  ],
  "version": 2
}
